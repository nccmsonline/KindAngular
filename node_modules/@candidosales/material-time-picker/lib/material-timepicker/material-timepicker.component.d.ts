import { EventEmitter, OnInit } from '@angular/core';
import { ITime } from '../w-clock/w-clock.component';
import { MatDialog } from '@angular/material';
export declare class MaterialTimePickerComponent implements OnInit {
    private dialog;
    label: string;
    appearance: string;
    userTime: ITime;
    color: string;
    revertLabel: string;
    submitLabel: string;
    change: EventEmitter<ITime>;
    constructor(dialog: MatDialog);
    ngOnInit(): void;
    time(): string;
    showPicker(): boolean;
    private emitChange;
}
