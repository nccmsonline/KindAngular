import { MiniSidebarDirective } from './mini-sidebar.directive';

describe('MiniSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new MiniSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
